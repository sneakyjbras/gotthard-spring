# Agent instructions

The brief asks for *"a short summary of agent instructions given."* Rather than
reconstruct that afterwards, every subagent's prompt is written here verbatim at
the moment it is spawned, alongside the model chosen for it and why.

| File | Task | Model |
|---|---|---|
| *(populated as agents are spawned)* | | |
