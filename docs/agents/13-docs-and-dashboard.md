# Agent 13 — Documentation site and Grafana dashboard

**Model:** Sonnet · **Wave:** 4.1 · **Scope:** `docs/`, `chart/files/`

Sonnet, and parallel to agent 12 because neither the documentation nor a dashboard
definition needs a cluster to exist.

## Instructions given
> Fill the MkDocs nav pages from what the repository actually contains, and author a
> Grafana dashboard as JSON for provisioning: the four golden signals from Micrometer,
> plus LLM token spend, which is the panel nobody else will have thought to build.
