# Agent 10 — Dashboard against the real API

**Model:** Sonnet · **Wave:** 3 · **Scope:** `frontend/src/`

Sonnet because the design system is settled, the API contract exists, and the port was
built for exactly this swap.

## Instructions given
> Replace the mock client with real fetch calls against the running backend, including
> the CSRF-then-login flow. Build the customer detail screen: activity per channel, the
> risk score with the rules that fired, and the evidence behind each.
